import 'package:flutter/material.dart';

class PostNews extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Post News'),
      ),
      body: Center(
        child: Text('Post News Page'),
      ),
    );
  }
}
